{
  "name": "@exxat/tools-visual-check",
  "version": "0.1.0",
  "private": true,
  "description": "Headless-browser visual + a11y check runner for the Exxat workspace. Used by .claude/agents/visual-review subagent + on-demand from any product dev server.",
  "type": "module",
  "scripts": {
    "check": "node run.mjs",
    "install:chromium": "playwright install chromium"
  },
  "dependencies": {
    "@axe-core/playwright": "^4.10.0",
    "playwright": "^1.49.0"
  }
}
