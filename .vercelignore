# Vite/SPA app in PCE — not part of the Next.js build, causes pages-router conflicts
apps/pce/admin/src/

# NOTE: do not re-add the library-* / new-library-item-form components here.
# They were excluded as untracked exxat-ui upgrade leftovers that pulled in
# react-router-dom. Both reasons are now obsolete: they are tracked, committed
# Next code imports them (sidebar/secondary-panel.tsx), and next.config.ts
# aliases react-router-dom to lib/react-router-compat.tsx. Excluding them only
# deleted files the build needs — "Module not found: @/components/library-*".

# Local worktree forests (13GB+) — dev/VCS clutter, never part of any build.
# Without these the CLI upload exceeds the 10mb request limit.
.claude/
.worktrees/
