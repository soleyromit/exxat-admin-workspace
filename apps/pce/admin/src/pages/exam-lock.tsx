import { ExamLockShowcaseClient } from "@/components/exam-lock-showcase-client"

/** Design OS — `/exam-lock` locked assessment shell showcase. */
export default function ExamLockPage() {
  return <ExamLockShowcaseClient />
}
