import { FocusWorkflowShowcaseClient } from "@/components/focus-workflow-showcase-client"

/** Design OS — `/focus-workflow` focus shell showcase. */
export default function FocusWorkflowPage() {
  return <FocusWorkflowShowcaseClient />
}
