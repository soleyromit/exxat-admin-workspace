{
  "framework": "nextjs",
  "installCommand": "pnpm install --ignore-scripts",
  "buildCommand": "pnpm build",
  "ignoreCommand": "case \"$VERCEL_GIT_COMMIT_REF\" in dev|main) exit 1;; esac; git fetch --depth=500 origin dev >/dev/null 2>&1 || exit 1; BASE=$(git merge-base FETCH_HEAD HEAD 2>/dev/null); [ -z \"$BASE\" ] && exit 1; git diff --quiet \"$BASE\" HEAD -- apps/pce/admin/ 2>/dev/null && exit 0 || exit 1"
}
