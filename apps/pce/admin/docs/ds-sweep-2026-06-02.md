# DS Sweep — apps/pce/admin
Scanned: 343 source files (PCE admin subset)
Date: 2026-06-02

---

## Summary

| Tier | Count | Biggest source |
|---|---|---|
| CRITICAL | 4 raw `<button>`, ~3 hex literals (non-backup) | exxat-prism-sheet, step-communication |
| HIGH — WCAG | 38 FA no aria-hidden, 8 no aria-label, 12 opacity, 3 click-no-focus | distribute wizard, sidebar |
| HIGH — banned CSS | 2 uppercase tracking-wide | minor |
| MEDIUM — DS gaps | 25 export-drawer, 11 missing ListPageTemplate, 3 card-masquerade, 2 status-badge | admin list pages |
| LOW — state | 19 DataTable missing emptyState + toolbarSlot | all admin list pages |

---

## CRITICAL — blocks done claim

### Raw `<button>` (4 real, 2 in .exxat-ui/backups — ignore backups)
| File | Line |
|---|---|
| components/pce/distribute-wizard/exxat-prism-sheet.tsx | 165 |
| components/pce/distribute-wizard/step-communication.tsx | 54 |
| components/ask-leo-sidebar.tsx | 325 |
| components/brand-color-picker.tsx | 320 |

Fix: `import { Button } from '@exxatdesignux/ui'` with explicit `variant` + `size`.

### Hex color literals — non-backup (3 real hits)
| File | Lines | Hex |
|---|---|---|
| components/pce/distribute-wizard/step-communication.tsx | 31, 31, 32 | `#FF5F57`, `#FEBC2E`, `#28C840` (traffic-light dots) |

These are decorative macOS traffic-light colors — no DS token maps to them. Acceptable as-is IF they're purely decorative (no text on them). Verify `aria-hidden="true"` is present on the containing element.

---

## HIGH — WCAG AA violations

### FA icons missing `aria-hidden="true"` (38 instances)
Run: `grep -rn "className=\"fa-\|fa " apps/pce/admin --include="*.tsx" | grep -v "aria-hidden\|backups"` — concentrated in distribute wizard, sidebar, hub components.

### Icon-only Button missing `aria-label` (8)
Run: `grep -rn "size=\"icon\"" apps/pce/admin --include="*.tsx" | grep -v "aria-label\|backups"`

### Clickable `<div>` without `focus-visible:ring` (3) — WCAG 2.1.1
| File | Line |
|---|---|
| templates/new/page.tsx | 177 |
| templates/[id]/page.tsx | 367 |
| components/ui/leo-icon.tsx | 654 |

Fix: either use DS `Button` (preferred) or add `tabIndex={0}`, `role="button"`, `onKeyDown` (Enter/Space), `focus-visible:ring-2 focus-visible:ring-ring`.

### `opacity-60/50/40` on text (12)
Potential WCAG 1.4.3 fail. Verify contrast for each.

---

## HIGH — Banned CSS patterns

### `uppercase tracking-wide` (2 instances)
Minor — grep `apps/pce/admin --include="*.tsx"` for `uppercase tracking-wide` to find exact files. Replace with `text-xs font-medium text-muted-foreground`.

---

## MEDIUM — DS coverage gaps

### Hand-rolled export drawer (25) → use `ExportDrawer`
`import { ExportDrawer } from '@exxatdesignux/ui'`

Admin list pages: offerings, terms, accommodations, permissions, students, courses, competencies, content-areas, standards.
Other: run-evaluation/page:605, templates/[id]/page:602, moderation/page:294, app-sidebar:380, settings-appearance-card:1109, table-properties/drawer:218, pce-modals:83, course-management-dialog:123, course-management-sheet:130, question-bank-picker-dialog:96, pdf-import-dialog:77, subject-picker-sheet:68, distribute-wizard (survey-preview-dialog, email-template-sheet), command-menu/index:111.

Note: `.exxat-ui/backups/` entries are stale — ignore.

### Missing `ListPageTemplate` (11) — all admin entity list pages
Files: admin/faculty, admin/offerings, admin/terms, admin/accommodations, admin/students, admin/courses, admin/competencies, admin/content-areas, admin/assessment-types, admin/standards, my-surveys.
All import DataTable directly; should use ListPageTemplate for built-in toolbar + view-switching.

### Card-shape masquerade (3)
| File | Line |
|---|---|
| surveys/[id]/page.tsx | 119 |
| components/ui/ai-thinking-surface.tsx | 14 |
| components/pce/distribute-wizard/survey-preview-dialog.tsx | 79 |

### Hand-rolled status badge (2)
| File | Line |
|---|---|
| admin/students/page.tsx | 41 |
| components/pce/pce-badges.tsx | 67 |

### Vendored `data-table/` + `table-properties/` directories
Migrate to `@exxatdesignux/ui` exports.

---

## LOW — State coverage

### DataTable missing `emptyState` prop (19 call sites)
### DataTable missing `toolbarSlot` prop (19 call sites)
All admin entity list pages lack both props.

### `overflow-hidden` with floating element (14)
Runtime verification needed — check popover/tooltip clip.
Files: run-evaluation/page:813, templates/[id]/page:253, analytics/page:673, ask-leo-composer:164, ask-leo-sidebar:277, settings-appearance-card:677, sidebar/nav-user:75, pce/surveys-hub:320, distribute-wizard (step-distribution:183, email-template-sheet:263, step-survey-design:155, exxat-prism-sheet:184, step-distribution-general:174).
