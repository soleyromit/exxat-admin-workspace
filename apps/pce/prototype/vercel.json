{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "cleanUrls": true,
  "trailingSlash": false,
  "buildCommand": "echo 'static site, no build needed'",
  "outputDirectory": ".",
  "installCommand": "echo 'no install needed'",
  "framework": null,
  "ignoreCommand": "R=$VERCEL_GIT_COMMIT_REF; case $R in dev|main) exit 1;; esac; git fetch -q --depth=99 origin dev || exit 1; B=$(git merge-base FETCH_HEAD HEAD) || exit 1; git diff --quiet $B HEAD -- apps/pce/prototype/ && exit 0 || exit 1",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "Referrer-Policy",
          "value": "strict-origin-when-cross-origin"
        }
      ]
    }
  ]
}
