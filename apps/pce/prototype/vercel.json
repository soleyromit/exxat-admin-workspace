{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "cleanUrls": true,
  "trailingSlash": false,
  "buildCommand": "echo 'static site, no build needed'",
  "outputDirectory": ".",
  "installCommand": "echo 'no install needed'",
  "framework": null,
  "ignoreCommand": "case \"$VERCEL_GIT_COMMIT_REF\" in dev|main) exit 1;; esac; git fetch --depth=500 origin dev >/dev/null 2>&1 || exit 1; BASE=$(git merge-base FETCH_HEAD HEAD 2>/dev/null); [ -z \"$BASE\" ] && exit 1; git diff --quiet \"$BASE\" HEAD -- apps/pce/prototype/ 2>/dev/null && exit 0 || exit 1",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "Referrer-Policy",
          "value": "strict-origin-when-cross-origin"
        }
      ]
    }
  ]
}
