# DS Sweep — apps/portal
Scanned: 5 route files (`app/`), 230 component files, 107 lib, 12 hooks, 8 contexts, 1 store, 28 legacy `src/` files
Date: 2026-07-17 · Branch: `fix/em-ds-gate-and-vercel-guard` (worktree `portal-ds-os-shell`)
Method: ds-sweep skill (audit scripts + WCAG/state greps + import-reachability closure from the two live routes `app/(shell)/page.tsx` and `app/(shell)/product/[id]/`). Read-only — nothing fixed.

**Verification notes (Pattern I):**
- `ds-adoption-audit.py --strict`, `ds-globals-audit.py --strict`, `architecture-audit.py --strict` all ran from worktree root, exit 0 — but **none of them scan apps/portal** (see C-2).
- The skill's single-line `size="icon"` grep produced **20 false positives**; a ±8-line proximity re-check found **zero** icon-only buttons genuinely missing `aria-label`.
- `grep` on this machine is aliased to ugrep and ignores `--exclude-dir` — `.exxat-ui/backups/` and `out/` were filtered by pipe; raw counts elsewhere may include them unless noted "live".

---

### CRITICAL — blocks done claim

| # | File | Line | Violation | Fix |
|---|---|---|---|---|
| C-1 | `components/ui/*.tsx` (≈55 one-line shims, e.g. `ui/tooltip.tsx`, `ui/button.tsx`) vs `app/(shell)/layout.tsx:3` | — | **Barrel vs subpath dual-module-instance risk.** The shims re-export DS *subpaths* (`export * from "@exxatdesignux/ui/components/tooltip"`) while live code imports the same primitives from the *main entry* (`TooltipProvider` from `@exxatdesignux/ui` in the shell layout; `Tooltip` via the subpath shim in `site-header.tsx:29`). `components/ui/sidebar.tsx:1-6` documents this exact failure ("separate module instance with its own React context → useSidebar must be used within a SidebarProvider") and was switched to the main entry — the other shims were NOT. This bug class already hit EM + PCE (build passes, route 500s). | Standardize every `components/ui/*` shim to `export * from "@exxatdesignux/ui"` (main entry) like `ui/sidebar.tsx`, or import everything from the barrel directly. Then runtime-verify tooltips/popovers in the shell. |
| C-2 | `scripts/ds-adoption-audit.py`, `scripts/ds-globals-audit.py` | — | **apps/portal is invisible to the DS gates.** The audits scan exam-management, patient-log, pce only; globals audit reports "3/3 admin apps clean" without portal. Portal has no automated DS enforcement at all. | Register `apps/portal` in the audit scripts' app roster so `--strict` covers it. |

### UNUSED-FILE DELETIONS — own tier (per triage: ~100+ vendored generated-starter files unreferenced by the two live routes)

Reachability closure from `app/layout.tsx` + `app/(shell)/{layout,page}.tsx` + `app/(shell)/product/[id]/{page,product-client}.tsx` reaches **113 files**. Everything else is dead weight for the live shell:

| Group | Count | Notes |
|---|---|---|
| `components/` unreferenced | **166 of 230** | Full generated-starter surface: all `design-system/*` previews (25), `data-views/*` list-page machinery (17 of 20), `templates/*` (7 of 8), `data-table/`, `table-properties/`, library/tokens/settings/dashboard clients, product-card variants, ~50 unused `ui/*` shims |
| `lib/` unreferenced | 76 of 107 | starter data/helpers |
| `contexts/` unreferenced | 5 of 8 | |
| `hooks/` unreferenced | 3 of 12 | |
| `src/` legacy Vite tree | 28 tsx + `src/App.tsx`, `src/routes.tsx`, `src/pages-legacy/*`, `vite.config.ts` | Nothing under `app/`/`components/` imports `src/` |
| `out/` build output **tracked in git** | 45 files | Static export committed; pollutes greps + repo |
| `.exxat-ui/backups/` **tracked in git** | 61 files | 3 CLI backup snapshots (2026-06-22 ×2, 2026-07-02) committed |

⚠️ **Deletion cautions (from MEMORY):** Next apps regenerate a Vite shell on install — do NOT "clean" blindly; validate with `next build` (not tsc) and run `git status` after ANY install. Several unreferenced files (library hub, templates, design-system docs) are the DS OS surface likely to be re-wired as portal grows — prefer *deliberate* deletion of what the roadmap won't use + `.gitignore` for `out/`, rather than a bulk purge.

### HIGH — DS violations in files REACHABLE from the live routes

| File | Line | Violation | Fix |
|---|---|---|---|
| `components/library-new-folder-sheet.tsx` | 182, 204 | raw `<button>` | DS `Button` with explicit `variant`+`size` |
| `components/ask-leo-view-toggle.tsx` | 74 | raw `<button>` (segmented toggle) | DS `Button` / `ButtonSegmentedControl` |
| `components/leo-landing-client.tsx` | 212 | raw `<button>` | DS `Button` |
| `components/search-recents-popover.tsx` | 71 | raw `<button>` | DS `Button` |
| `components/data-views/library-folder-tree-branch.tsx` | 273 | raw `<button>` (tree row) | DS `Button asChild` or SidebarMenuButton pattern |
| `components/sidebar/secondary-nav.tsx`, `sidebar/app-sidebar.tsx`, `sidebar/sidebar-collapsed-search-button.tsx` | — | raw `<button>` in vendored starter sidebar files | Verify against generated-starter source at :4000 before rewriting — vendored chrome may be canonical DS composition |
| `components/data-views/library-folder-tree-branch.tsx` | 270 | `<i class="fa-solid fa-check">` without own `aria-hidden` (parent `<span aria-hidden>` covers it — bare attr, renders `aria-hidden="true"`) | Nit: move `aria-hidden="true"` onto the `<i>` per house rule |

WCAG in live routes themselves: **clean** — `page.tsx:27` and `product-client.tsx:214` FA icons both carry `aria-hidden="true"`; zero icon-only buttons missing `aria-label` anywhere (proximity-verified); no raw `<button>`/`<table>`, no hex, no toast in `app/`.

### MEDIUM — banned patterns / DS coverage (mostly vendored, verify vs localhost:4000 before "fixing")

| File | Line | Issue | Fix |
|---|---|---|---|
| `app/globals.css` | 157 | `color-mix(in oklch, …)` for `--product-icon-backdrop` (banned-pattern grep; only *live* hit — others are in `out/` bundles) | Replace with a DS token or precomputed oklch value |
| `components/sidebar/{secondary-nav.tsx:157,306, app-sidebar.tsx:1183,1544, sidebar-drill-in.tsx:150, leo-sidebar-drill-in-panel.tsx:123}`, `components/library-secondary-nav.tsx:481` | — | `uppercase tracking-wide/-wider` section labels in reachable files | Sidebar group labels use `text-sidebar-section-label` token and may be DS-canonical — visual-diff vs :4000 before changing; `library-secondary-nav.tsx:481` is the suspect one |
| ~30 files incl. reachable `command-menu-content.tsx`, `sidebar/nav-main.tsx`, `sidebar/nav-user.tsx`, `sidebar/app-sidebar.tsx`, `sidebar/secondary-panel.tsx`, `hooks/use-secondary-panel-hub-nav.ts` | — | `react-router-dom` imports (**^7.18.1 is a real dep in a Next app**, `package.json:25`; `lib/router-compat.tsx` exists) — **known triaged issue** | When touched, migrate to `next/navigation` or route through `router-compat` |
| `components/chart-heatmap.tsx` (unused) | 61–80 | 14 hardcoded hex — but all are *fallback args* to `readChartToken("--brand-color", …)` | Low risk; deletes with tier U or keep as token fallbacks |
| `components/settings-appearance-card.tsx` (unused) | 115–393 | 18 hardcoded hex (theme-preview swatches incl. contrast themes) | Deletes with tier U; arguably legitimate preview data |
| `src/styles/exxat-composer.css` (legacy) | — | 3 hardcoded hex | Deletes with `src/` tier |
| Duplicate sidebars: `components/app-sidebar.tsx` (live shell, imports barrel) vs `components/sidebar/app-sidebar.tsx` (1.5k-line starter, reachable via `@/components/sidebar` barrel) | — | Two AppSidebar implementations coexist | Consolidate once library hub is wired or delete the starter one with tier U |
| FA `<i>` missing `aria-hidden` in **unused** files: `new-library-item-form.tsx` (642, 660, 2324, 3025, 3055), `tokens-themes-section.tsx` (311, 321), `settings-client.tsx:97`, `tinted-icon-disc.tsx:50`, `library-folder-picker-panel.tsx:105`, `data-views/table-cells.tsx:411`, `src/pages-legacy/*` (4) | — | 16 true instances (tag-scoped check) | Fix only if the file survives tier U |

### LOW — state coverage & triaged items

| Item | Status |
|---|---|
| Live routes state coverage | OK for current scope: data is static (`lib/products`), `product-client.tsx:117` calls `notFound()` for unknown ids, `generateStaticParams` present. No async fetch → no loading/empty/error states required yet. |
| `leo-drawer.tsx` uppercase labels (57, 132, 164) + hand-rolled avatar | **Parked by Romit** (Leo work deprioritized) — do not fix now |
| `text-[8–11px]` sub-12px type — 11 live instances across 7 files (`library-os-folder-view` ×3, `hub-tree-panel-view` ×2, `folder-details-shell` ×2, +4) | **All in unused starter files** (triaged known issue); zero in reachable code except none — resolves with tier U |
| `opacity-40/50/60` — 23 live hits; reachable: `ask-leo-composer.tsx` 377/381/386/425 (decorative `aria-hidden` icons + disabled state) | Contrast risk minimal; revisit only if Leo composer ships |
| `DataTable` without `emptyState` (`catalog-live-previews.tsx:358`, `component-doc-api-table.tsx:74`, `library-table.tsx`) | All unused starter files — resolves with tier U |
| `toast(` usage | None (clean) |
| Raw visual `<table>` (`design-system/token-doc-preview.tsx:200`) | Unused doc-preview file; other `<table>` hits are `sr-only` accessibility tables (correct pattern) |

---

## Suggested fix order
1. C-1 shim standardization + runtime tooltip verify (one mechanical pass, prevents a known 500 class)
2. C-2 register portal in audit scripts (keeps everything else honest)
3. Tier U: decide keep-list with Romit → delete `src/` + `out/` + `.exxat-ui/backups` + agreed component set → `next build` + `git status` after install
4. HIGH raw-button fixes in the 5 reachable non-chrome files
5. MEDIUM only for files that survive tier U

*Which CRITICAL items should we fix first?*
